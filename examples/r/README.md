# R Examples

R/ggplot2 examples using `theme_chartsmith()`:

- `bar_horizontal.R` — Top 10 design concerns (Figure 4.9)
- `sample_output.R` — Revenue vs target (end-to-end demo)

Run from the repo root: `Rscript examples/r/<script>.R`

Requires `ggplot2` and `scales`.
