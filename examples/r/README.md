# R Examples

R examples are coming soon. The Chartsmith skill generates R/ggplot2 code using `theme_chartsmith()`, but shipped examples are Python-only for now.

See `examples/python/` for working examples that demonstrate the design principles.
